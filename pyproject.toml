[project]
name = "classify-justify"
version = "0.1.0"
description = "Train an X-ray defect classifier, explain it with many attribution methods, and measure whether the explanations can be trusted."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Youssef Assis" }]
keywords = ["explainable-ai", "xai", "attribution", "captum", "defect-detection", "x-ray"]

dependencies = [
    "torch>=2.2",
    "torchvision>=0.17",
    # Captum supplies the gradient and surrogate attribution methods; the CAM family
    # it does not ship is implemented in explain/cam.py.
    "captum>=0.7",
    "pillow>=10.0",
    # Only for rendering explanation grids in `cjust explain`.
    "matplotlib>=3.7",
    # The CA bundle for the dataset download: a stock python.org install on macOS
    # ships no usable certificate store and fails to fetch over HTTPS without it.
    "certifi>=2024.2",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.6"]

[project.scripts]
cjust = "classify_justify.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/classify_justify"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = ["error::DeprecationWarning"]
